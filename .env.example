# Copie para `.env` (ignorado pelo git) e preencha.

# Credenciais do Kaggle — use UM dos dois formatos.
KAGGLE_USERNAME=
KAGGLE_KEY=
# KAGGLE_API_TOKEN=

# Opcional: outro YAML de configuração (padrão: configs/global_config.yaml)
# CONFIG_PATH=configs/global_config.yaml
